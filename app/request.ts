/** Never silently retry financial writes: a timed-out request may have committed. */
export async function request(url:string,options:RequestInit={},timeout=20000){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),timeout);try{return await fetch(url,{...options,signal:controller.signal})}catch(error){if(controller.signal.aborted)throw new Error(options.method&&options.method!=='GET'?'The request timed out. Refresh your records before retrying; the change may already have been saved.':'The connection is taking too long. Please try again.');throw error}finally{clearTimeout(timer)}}
